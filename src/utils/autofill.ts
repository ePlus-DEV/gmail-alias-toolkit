export const EMAIL_INPUT_SELECTOR = ['input[type="email"]','input[name*="email" i]','input[id*="email" i]','input[autocomplete="email"]','input[placeholder*="email" i]'].join(',');
export function setNativeValue(element: HTMLInputElement | HTMLTextAreaElement, value: string) { const valueSetter=Object.getOwnPropertyDescriptor(element,'value')?.set; const prototype=Object.getPrototypeOf(element); const prototypeValueSetter=Object.getOwnPropertyDescriptor(prototype,'value')?.set; if (prototypeValueSetter && valueSetter !== prototypeValueSetter) prototypeValueSetter.call(element,value); else if (valueSetter) valueSetter.call(element,value); else element.value=value; element.dispatchEvent(new Event('input',{bubbles:true})); element.dispatchEvent(new Event('change',{bubbles:true})); }
export function findBestEmailInput(): HTMLInputElement | null { const active=document.activeElement; if(active instanceof HTMLInputElement && active.matches(EMAIL_INPUT_SELECTOR)) return active; return document.querySelector<HTMLInputElement>(EMAIL_INPUT_SELECTOR); }
export function autofillEmail(alias:string): boolean { const input=findBestEmailInput(); if(!input) return false; input.focus(); setNativeValue(input, alias); return true; }
